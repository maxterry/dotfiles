dotfiles
========

Shell configuration.

Installation
------------

    > git clone git://github.com/1083/dotfiles.git
    > cd dotfiles
    > cat .profile >> ~/.profile
    > cat .vimrc >> ~/.vimrc
    > cat .gitconfig >> ~/.gitconfig
    > cat .gitignore >> ~/.gitignore
    > source ~/.profile
