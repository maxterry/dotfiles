dotfiles
========

Config.

Installation
------------

    > cd [location]
    > git clone git://github.com/maxwellterry/dotfiles.git
    > cd dotfiles
    > ln -nfs .profile ~/.profile
    > ln -nfs .vimrc ~/.vimrc
    > ln -nfs .gitconfig ~/.gitconfig