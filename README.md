dotfiles
========

Shell configuration.

Installation
------------

    > cd ~
    > git clone git://github.com/1083/dotfiles.git
    > cd dotfiles
    > ln .profile ~/.profile
    > ln .vimrc ~/.vimrc
    > ln .gitconfig ~/.gitconfig
    > ln .gitignore ~/.gitignore
