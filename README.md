dotfiles
========

Config.

Installation
------------

    > cd ~
    > git clone git://github.com/maxwellterry/dotfiles.git
    > cd dotfiles
    > ln .profile ~/.profile
    > ln .vimrc ~/.vimrc
    > ln .gitconfig ~/.gitconfig
    > ln .gitignore ~/.gitignore